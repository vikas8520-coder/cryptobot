# --- SECRETS (never commit) ---
telegram.conf
telegram.conf.bak
*.bak
# per-bot API creds (jwt_secret_key, ws_token, password)
*.secret.json
# localhost REST API passwords for the helper tools
local_secrets.py

# --- Python env / caches ---
.venv/
__pycache__/
*.pyc

# --- Trade databases (runtime data) ---
*.sqlite
*.sqlite-shm
*.sqlite-wal

# --- Logs ---
*.log
*.err

# --- Bulky / generated ---
user_data/data/
user_data/hyperopt_results/
user_data/backtest_results/
user_data/plot/

# --- Constantly-changing runtime state ---
*_state.json

# --- Runtime/generated data (churns; regenerated by bots/jobs) ---
activity_feed.jsonl
*_history.csv
risk_backtest_equity.csv
*_board.json

# --- ApeX (key/secret/passphrase from on-chain registration) ---
apex_creds.json

# weekly keep/kill scorecard (append-only runtime; script is source of truth)
paper_scorecard.csv
paper_scorecard.out
paper_scorecard.err
