{
  "strategy_name": "TrendFollowHoptSL04",
  "params": {
    "roi": {
      "0": 10
    },
    "max_open_trades": {
      "max_open_trades": 2
    },
    "buy": {
      "buy_adx": 25,
      "ema_fast": 20,
      "ema_slow": 50
    },
    "stoploss": {
      "stoploss": -0.04
    },
    "trailing": {
      "trailing_stop": true,
      "trailing_stop_positive": 0.03,
      "trailing_stop_positive_offset": 0.06,
      "trailing_only_offset_is_reached": true
    }
  },
  "ft_stratparam_v": 1,
  "export_time": "2026-07-23T00:00:00+00:00",
  "_comment": "audit 2026-07-23: A/B probe json for the SL sweep. Copy of TrendFollowHopt.json with stoploss -0.04 (vs baseline -0.08). Freqtrade auto-loads <StrategyName>.json and a JSON-pinned stoploss BEATS the strategy class attribute, so the sibling class-attr override (TrendFollowHoptSL04.py) was silently ignored; pinning it here is the fix. Backtest-only, not wired to any live bot."
}
