[pytest]
testpaths = tests
addopts = -q --strict-markers
# The bots' modules live at the repo root and import each other by bare name
# (import state_io), so the root must be on sys.path when pytest runs.
pythonpath = .
